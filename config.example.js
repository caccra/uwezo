// Copy this file to  config.js  and fill in the two values from your Supabase
// project:  Project Settings -> API.
//
//   config.js is git-ignored. The anon / publishable key is safe to expose in
//   the browser — Row-Level Security in the database is what protects the data.

window.UWEZO_CONFIG = {
  SUPABASE_URL: "https://YOUR-PROJECT-ref.supabase.co",
  SUPABASE_ANON_KEY: "YOUR-ANON-OR-PUBLISHABLE-KEY",
};
